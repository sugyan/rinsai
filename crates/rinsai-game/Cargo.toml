[package]
name = "rinsai-game"
description = "A shogi game with legality-gated moves, undo, and rule-decided outcomes: 詰み, 千日手, 連続王手の千日手"
# Versioned apart from the workspace: this crate is publication-intended and
# the engine crates are not, so its version follows its own API.
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
shogi_core.workspace = true
shogi_legality_lite.workspace = true
shogi_official_kifu.workspace = true
shogi_usi_parser.workspace = true
